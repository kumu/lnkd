"use strict";
exports.__esModule = true;
var index_1 = require("../../index");
test('transform', function () {
    var graph = index_1["default"]().compose();
    (linkedin({ TOKEN: 'ABC' }));
    add('one'),
        add('two'),
        connect('one', 'two');
});
index_1["default"]().concat(linkedin(KEY), twitter(KEY));
graph.add(linkedin(KEY), function (component, collection) {
});
graph.add('one');
graph.add('two');
graph.connect('one', 'two');
graph.rename('name', 'label');
graph.select('foo')
    .field('foo', 'bar')
    .style('color', 'red');
graph.transform(select('foo'), field('foo', bar));
graph.calc(reciprocity);
var r = reciprocity(graph);
//# sourceMappingURL=index.test.js.map