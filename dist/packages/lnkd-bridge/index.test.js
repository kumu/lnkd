"use strict";
exports.__esModule = true;
test('bridge', function () {
    // let graph = lnkd({
    //   nodes: [
    //     {id: "Bob", type: "Employee"},
    //     {id: "Joe", type: "Employee"},
    //     {id: "Acme Inc", type: "Organization"}
    //   ],
    //
    //   // edge shape?
    //   // { source, target }
    //   // { from, to }
    //   // [i, j, { meta }]
    //   edges: [
    //     {from: "Bob", to: "Acme Inc"},
    //     {from: "Joe", to: "Acme Inc"}
    //   ]
    // })
    //
    // lnkd(graph)
    //
    // // don't provide anything
    //
    // validate(graph, component => Boolean(component.id))
    //
    // lnkd.remove()
    // lnkd.transform(bridge(options))
    //
    // let results = bridge(graph, graph.nodes.select(node => node.type === "Organization"));
    //
    // graph.neighbors(node)
    //
    // graph
    //   .transform(
    //     bridge(graph.nodes.select(node => node.type === "Organization"))
    //   )
    // let result = lnkd(graph).bridge(select("company")).graph();
    //
    // bridge(graph, );
    expect(false).toBe(true);
});
//# sourceMappingURL=index.test.js.map